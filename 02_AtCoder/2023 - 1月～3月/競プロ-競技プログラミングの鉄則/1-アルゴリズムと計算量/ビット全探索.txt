コラム
ビット全探索を使った部分和問題。
O(2**N)

「使うか使わないかの全探索」

for文の構築をN回だけ作らないといけない、
処理がO(2**N)の問題の場合、
　どうやってコードを書いてみようという話で、

そのnは使うのか使わないのか
[0,0,0,1,1,1,1]
という風にメモすると、わかりやすい。ということ。
programも、この記載したメモを使って一回のfor文を回すだけで終了する。



例）
a_list = list(range(10))
bit    = [[0 for _ in range(10)] for i in range(10)]




〇わからなかったところ
numpyを使わない状態で
ans = sum(a_list and bit[0])的な処理
bitが１となっているところだけの和を計算する方法について。